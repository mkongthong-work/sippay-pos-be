# ---- ความลับ/ค่าตั้งค่าเฉพาะเครื่อง ----
.env

# ---- ฐานข้อมูล SQLite (ข้อมูลจริงของร้าน ไม่ควรอยู่ใน repo) ----
*.db
*.db-journal

# ---- รูปสลิปโอนเงินที่พนักงานอัปโหลด (ข้อมูลจริงของร้าน ไม่ควรอยู่ใน repo) ----
uploads/

# ---- Go build artifacts ----
pos-backend
tmp/

# ---- macOS ----
.DS_Store

# ---- Editors ----
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
